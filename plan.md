# 框架优化计划（待确认后执行）
目标：让后续对任意公司的分析更具普适性，降低“单边风险放大/利好遗漏/参数硬伤/行业错配”导致的误判概率，并把关键结论从“绝对断言”改为“证据强度+不确定性管理”的决策输出。

## 0. 设计原则（先统一）
- 结论分层：把“事实→推断→建议”分开写；建议必须带条件与触发器。
- 双向对称：每个重大风险必须同时给出“证据/后续进展/缓释因素/剩余不确定性”四件套；每个重大利好必须给出“证据/可持续性/反例条件”。
- 行业适配：估值、流动性、周期判断必须先做“行业类型识别”，再选方法；不适配的方法默认禁用或降权。
- 参数闸门：对会导致结论翻转的关键参数（股本、分红、ROE口径、基准价、受限资金、Capex假设）先做一致性校验，通过后才允许进入推导。
- 可追溯：所有关键数字必须能回溯到 data_pack 的章节/行项，且口径写清楚（法定加权ROE/期末摊薄、实施/预案、可用现金/货币资金等）。

## 1. 框架层面要补的“四个模块”
### 1.1 事件与治理：从“一票否决”改为“证据分级+后续核验”
交付物（Prompt/规则层）：
- 事件分级矩阵（Hard Veto / Soft Veto / Observation / Info Only），每类明确触发条件与所需证据。
- “后续进展核验”步骤：凡涉及实控人/治理负面事件，必须检查是否存在监管立案/处罚/司法进展、公司公告对经营影响说明、管理层稳定性信号（任职年限/离职）、质押与减持等量化缓释项。
- 输出格式强制化：风险条目必须包含【事件→证据→后续→影响路径→缓释→未决问题→需要继续跟踪的公告类型】。

代码侧（数据支持）：
- 在 data_pack_market 增加“治理缓释面板”：质押率、实控人持股、近年减持/增持/回购、审计意见、审计师变更次数等（已有接口优先直接落表）。
- 若仓内数据源无法覆盖“事件后续/监管动作”，先提供一个“事件输入槽位”（结构化占位区），允许用户手工补一段关键公告摘要后再跑 Phase3；后续再决定是否接入公告数据源。

### 1.2 估值：做“行业方法选择器”，并输出区间与敏感性
交付物：
- 行业类型识别（轻资产/重资产/金融/公用事业/资源品强周期/成长制造等）→ 对应启用的估值方法集合（PB-ROE、EV/EBITDA、DCF、股息率隐含价等）与禁用方法（例如重资产行业默认禁用净流动资产法作为主基准）。
- 基准价从“单一均值”改为“方法集合的可解释区间”：输出每个方法的结果、适配性说明、以及综合区间（而不是强行算术平均）。
- Capex周期拐点识别：新增“扩张期/成熟期”判断，并对 Owner Earnings 中 Capex 假设做情景（扩张期保持/回落到维持性Capex/逐年下行）敏感性。

代码侧（数据支持）：
- 在预计算段落（估值与安全边际的输入）增加行业字段传递，让脚本能按行业开关估值方法并生成“区间”。
- 新增关键假设表：Capex、D&A、营运资本变动、分红（实施+预案）口径，作为 Phase3 只读锚点。

### 1.3 行业与周期：把“强周期”拆成可验证的驱动因子
交付物：
- 强周期行业必须输出“盈利驱动拆解”：至少包含价差/成本曲线位置/供给约束/需求弹性四项，缺数据则标注为“缺口”而非直接下结论。
- 增加“供给端壁垒”检查清单：审批/产能投放节奏/政策限制对新增供给的影响（可用通用模板，具体事实需数据源或用户补充）。

代码侧（数据支持，分两步走）：
- 第一步：仅做结构化模板与缺口管理（无外部数据也可运行）。
- 第二步（可选）：接入外部数据源（若仓内已有）来补齐价差/行业供给信息；若没有则不强行做自动化。

### 1.4 风险与财务：用“行业基准”替代通用阈值
交付物：
- 把流动比率/速动比率等通用阈值改为“行业对标+动态偿债能力”：经营现金流覆盖短债、利息保障倍数、授信余额（若无则提示缺口）。
- 安全环保/处罚类信息改为“时点化”：区分历史、近一年趋势、整改投入与结果；缺数据则提示“不确定”。

代码侧（数据支持）：
- 增加“动态偿债面板”：OCF、短债（含一年内到期非流动负债）、利息费用/EBIT 或 EBITDA（如可取）。
- 对“受限资金”形成硬口径：可用现金必须扣除 P2 受限货币资金（已在框架规则层要求）。

### 1.5 原料/行业信息：可落地查询 + 核验 + 兜底（Web优先）
背景：你当前没有自建“原料/行业数据库”，因此第一阶段以“网络信息查询”为主，同时必须保证时效性与准确性。

交付物（框架与规则层）：
- 分层数据源策略（优先级从高到低）：
  - Level A：官方与一手披露（交易所公告/年报与附注/政策原文/监管网站/统计局与协会公开数据）
  - Level B：权威二手（券商研报、机构月报/白皮书、知名资讯数据库的公开页面）
  - Level C：行业网站与报价平台（作为参考，需要交叉核验与口径标注）
  - Level D：手工兜底（结构化输入槽位，必须带来源与日期）
- 核验规则（所有 Web 引用必须满足）：
  - 时效：每条原料/产品价格、政策与行业供给信息都必须标注“取数日期/数据区间”；超过阈值视为过期并降低置信度
  - 口径：强制标注“含税/不含税、出厂/到厂/现货/长协、地区、频率（日/周/月）”；不可比则不计算价差
  - 交叉验证：关键价格/价差至少需要 2 个独立来源一致（或能解释差异），否则标注“不确定性高”并停止外推
  - 可追溯：报告中每个外部事实必须能回溯到“URL/标题/发布日期/摘录要点”（以引用清单形式输出）
- 缺口管理与结论保护：
  - 原料/行业关键字段缺失时，不允许用猜测替代；必须输出缺口清单，并将相关判断的置信度下调
  - 对“强周期”结论：若价差/供给约束/需求弹性无法验证，则只允许输出“待验证假设”，禁止下绝对结论

落地方式（不依赖自建数据库）：
- 增加一个“原料与行业信息结构化面板”，字段包含：原料名称/产品名称、价格、区间波动、口径、来源、日期、是否可比、缺口标记
- 增加“手工补录槽位”（结构化字段而非自由文本），用于你补充：原料采购占比、成本分位、政策约束摘要、弹性参数等
- 增加“Web 信息缓存（可选）”：在本地保存查询到的原始摘要与来源，用于复现与回归测试；缓存必须带时间戳与来源

## 2. 落地到仓库的改动清单（确认后执行）
### 2.1 Prompt/规则文件（提升普适性为主）
- 更新因子1A：将“控股股东重大负面信号”拆成可证据化条款；新增“事件后续核验”；将“一票否决”从“疑似事件”收敛到“已触发监管/司法或明确经营影响”的硬证据。
- 更新因子4：加入行业估值方法选择器与“区间+敏感性”输出规范；禁止不适配方法参与综合价。
- 更新 Phase3 执行器：强制引用 data_pack 的锚点表；最终结论改为【建议（买入/观察/回避）+置信度（高/中/低）+触发器清单】。
- 增加 Web 引用规范：必须输出“引用清单（来源+日期+口径+结论关联）”；不满足核验规则则不得用于关键推导。

### 2.2 数据包生成脚本（把关键锚点写死，减少LLM自由发挥）
- 在 market 数据包中新增：行业标签、资本开支趋势摘要、动态偿债面板、治理缓释面板、关键假设表。
- 增加“数据质量闸门”：对关键参数做一致性/数量级校验，异常时在数据包显著位置提示，并要求 Phase3 降低置信度或停止推导。

### 2.3 配置化（让行业适配可扩展）
- 新增一个行业规则配置文件（例如 `config/industry_rules.yaml`）：定义行业→启用估值方法/阈值/禁用项。
- 允许用户覆盖：同一公司可手动指定“行业类型”或估值方法开关，用于应对行业归类不准确。
 - 行业分类建议双轨制：
   - 基础层：`stock_basic.industry` → `industry_attribute.yaml`（映射到“估值适配行业类型+核心驱动因子+禁用方法”）
   - 覆盖层：`custom_industry_mapping.yaml`（股票代码→自定义行业类型，优先级更高）
 - 原料/行业信息补录配置：定义结构化字段、必填项（来源/日期/口径）、以及“缺口字段清单”生成规则

### 2.4 测试与回归（保证不再出现“硬伤”）
- 增加测试用例：股本/分红/ROE/基准价/受限资金/Capex假设的闸门规则可触发且提示明确。
- 为“重资产行业禁用净流动资产法主基准”增加一个快照测试，确保输出模板稳定。

## 3. 验收标准（你确认后我按这些验收）
- 报告不再因“信息不完整”直接一票否决：必须先给出证据分级、后续核验结果与不确定性清单。
- 任一关键参数异常（数量级/口径冲突）会在数据包和最终结论处被明确提示，并降低置信度或阻断结论外推。
- 估值输出为“方法-区间-敏感性”，不再用不适配方法拉低综合基准价。
- 行业判断至少包含“驱动拆解+缺口管理”，不再只给标签式断言。
- 数据缺口标注率 100%：原料/行业数据缺失必须显式列出，不允许默认为 0 或跳过说明。
- Web 引用可追溯率 100%：每条外部事实都有来源+日期+口径；无法核验的事实不得进入关键推导。

## 4. 需要你确认的两项决策（确认后我开始改代码与配置）
- 公告/监管动态第一阶段：只做“手工结构化槽位”还是同时做“标题+日期”的轻量 Web 抓取？
- Web 信息的准确性策略：是否要求“关键价格/政策至少双来源交叉验证”作为硬门槛（不满足则禁止用于结论）？
